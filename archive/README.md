# Archive

This directory preserves earlier scripts, appendices, plots, and statistical outputs from the sentiment-only version of the project.

The active, cleaned pipeline lives in `src/`. Large local data artifacts and checkpoints are kept under ignored archive paths so they remain available on this machine without being reintroduced into the public repository surface.

